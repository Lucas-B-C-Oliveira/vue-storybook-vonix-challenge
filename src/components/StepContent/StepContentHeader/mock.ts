import type { StepContentHeaderProps } from '@/components/StepContent/StepContentHeader/StepContentHeader.vue'

const queue: StepContentHeaderProps = {
  title: 'Fila'
}

const file: StepContentHeaderProps = {
  title: 'Arquivo'
}

const fileMapping: StepContentHeaderProps = {
  title: 'Mapeamento'
}

export const Mock = {
  queue,
  file,
  fileMapping
}