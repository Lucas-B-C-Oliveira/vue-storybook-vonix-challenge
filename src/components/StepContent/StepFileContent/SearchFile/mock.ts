import { type SearchFileProps } from '@/components/StepContent/StepFileContent/SearchFile/SearchFile.vue'

const primary: SearchFileProps = {
  iconName: 'magnifyingGlass',
  placeholderText: 'Pesquisar por arquivo'
}

export const Mock = {
  primary,
}