import { type FileContentButtonProps } from '@/components/StepContent/StepFileContent/FileContentButton/FileContentButton.vue'

const leftButton: FileContentButtonProps = {
  iconName: 'leftCaret'
}

const rightButton: FileContentButtonProps = {
  iconName: 'rightCaret'
}

export const Mock = {
  leftButton,
  rightButton
}